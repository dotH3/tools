{
  "name": "tools",
  "version": "0.1.0",
  "private": true,
  "description": "A small collection of browser-based tools, styled with Tailwind CSS.",
  "scripts": {
    "build:css": "node scripts/generate-tailwind.js",
    "watch:css": "node scripts/generate-tailwind.js && echo 'Re-run build:css after editing tailwind.config.js or src/input.css'",
    "dev": "node scripts/generate-tailwind.js && python3 -m http.server 8000"
  },
  "keywords": ["tailwindcss", "tools", "image-editor"],
  "license": "MIT"
}
